#!/usr/bin/env ruby

require File.dirname(__FILE__) + '/../lib/bottlerocket/generator'

if ARGV.empty?
  STDERR.puts %{
example: bottlerocket my_shiny_new_site
  }
else
  generator = Generator.new(ARGV.first)
  generator.create_directory
  generator.copy_template
end
